import React, { Component } from 'react'

// rcc shortcut to bring in the component as a class instead of a function
export default class Header extends Component {
  render() {
    return (
      <div>Header</div>
    )
  }
}
