import React, { Component } from 'react'
//rce shortcut creates class component and export default at the bottom
export class Header extends Component {
  render() {
    return (
      <div>Header</div>
    )
  }
}

export default Header
